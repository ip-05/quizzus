[server]
secure = false
domain = "localhost"
base = "http://localhost:1234"
host = "localhost"
port = 1234

[google]
client_id = "id"
client_secret = "secret"

[frontend]
base = "http://localhost:4321"

[secrets]
jwt = "jwt"